[{"_id":"69a64d16ff996647cc9a6be3","name":"Two Pointers","category":"Array / String","description":"Use two indices moving toward each other (or in the same direction) to solve problems in-place or find pairs.","spaceComplexity":"O(1)","tags":["array","in-place","sorted"],"template":"def two_pointers(arr, target):\n    left, right = 0, len(arr) - 1\n    while left < right:\n        current = arr[left] + arr[right]\n        if current == target:\n            return [left, right]\n        elif current < target:\n            left += 1\n        else:\n            right -= 1\n    return []\n\n# Same-direction variant (remove duplicates):\ndef remove_duplicates(arr):\n    if not arr:\n        return 0\n    slow = 0\n    for fast in range(1, len(arr)):\n        if arr[fast] != arr[slow]:\n            slow += 1\n            arr[slow] = arr[fast]\n    return slow + 1","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Sorted array, find pair with target sum, remove duplicates, palindrome check, container with most water."},{"_id":"69a64d17ff996647cc9a6be4","name":"Sliding Window","category":"Array / String","description":"Maintain a window (subarray/substring) and expand/shrink it to find an optimal subarray satisfying a constraint.","spaceComplexity":"O(k) where k = window size or charset","tags":["array","string","subarray"],"template":"from collections import defaultdict\n\ndef sliding_window(s, k):\n    left = 0\n    window = defaultdict(int)  # or use a sum variable\n    result = 0\n\n    for right in range(len(s)):\n        # 1. Expand: add s[right] to window\n        window[s[right]] += 1\n\n        # 2. Shrink: while window is invalid\n        while len(window) > k:          # condition depends on problem\n            window[s[left]] -= 1\n            if window[s[left]] == 0:\n                del window[s[left]]\n            left += 1\n\n        # 3. Update result\n        result = max(result, right - left + 1)\n\n    return result\n\n# Fixed-size window (max sum of k elements):\ndef max_sum_k(arr, k):\n    window_sum = sum(arr[:k])\n    best = window_sum\n    for i in range(k, len(arr)):\n        window_sum += arr[i] - arr[i - k]\n        best = max(best, window_sum)\n    return best","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Longest/shortest subarray or substring with a condition, max sum of k elements, minimum window substring."},{"_id":"69a64d17ff996647cc9a6be5","name":"Binary Search","category":"Array / String","description":"Eliminate half the search space each iteration. Works on sorted inputs or when there is a monotonic predicate.","spaceComplexity":"O(1)","tags":["sorted","divide-and-conquer"],"template":"# Standard binary search\ndef binary_search(arr, target):\n    lo, hi = 0, len(arr) - 1\n    while lo <= hi:\n        mid = lo + (hi - lo) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1\n\n# Find leftmost position (first occurrence / lower bound):\ndef lower_bound(arr, target):\n    lo, hi = 0, len(arr)\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if arr[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n    return lo\n\n# Binary search on answer, find minimum x where feasible(x) is True:\ndef binary_search_answer(lo, hi):\n    def feasible(mid):\n        return True  # implement condition\n\n    while lo < hi:\n        mid = lo + (hi - lo) // 2\n        if feasible(mid):\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo","timeComplexity":"O(log n)","updatedAt":{},"whenToUse":"Sorted array target search, search in rotated array, find first/last occurrence, binary search on answer."},{"_id":"69a64d17ff996647cc9a6be6","name":"Fast & Slow Pointers","category":"Linked List","description":"Floyd's cycle detection. Slow moves 1 step, fast moves 2. They meet iff there is a cycle.","spaceComplexity":"O(1)","tags":["linked-list","cycle"],"template":"# Detect cycle\ndef has_cycle(head):\n    slow, fast = head, head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            return True\n    return False\n\n# Find cycle start (Floyd's)\ndef cycle_start(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow, fast = slow.next, fast.next.next\n        if slow == fast:\n            break\n    else:\n        return None  # no cycle\n    slow = head\n    while slow != fast:\n        slow, fast = slow.next, fast.next\n    return slow\n\n# Find middle of linked list\ndef find_middle(head):\n    slow, fast = head, head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    return slow","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Detect cycle in linked list, find cycle start, find middle of list, find k-th element from end."},{"_id":"69a64d17ff996647cc9a6be7","name":"BFS","category":"Graph / Tree","description":"Breadth-first search explores level by level. Use a queue. Optimal for shortest path in unweighted graphs.","spaceComplexity":"O(V)","tags":["graph","tree","shortest-path"],"template":"from collections import deque\n\ndef bfs(graph, start, end=None):\n    visited = {start}\n    queue = deque([(start, 0)])   # (node, distance)\n    while queue:\n        node, dist = queue.popleft()\n        if node == end:\n            return dist\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append((neighbor, dist + 1))\n    return -1  # not reachable\n\n# Level-order tree traversal\ndef level_order(root):\n    if not root:\n        return []\n    result, queue = [], deque([root])\n    while queue:\n        level = []\n        for _ in range(len(queue)):\n            node = queue.popleft()\n            level.append(node.val)\n            if node.left:  queue.append(node.left)\n            if node.right: queue.append(node.right)\n        result.append(level)\n    return result\n\n# Multi-source BFS (e.g. all 0s spread simultaneously)\ndef multi_source_bfs(grid):\n    queue = deque()\n    for r in range(len(grid)):\n        for c in range(len(grid[0])):\n            if grid[r][c] == 0:\n                queue.append((r, c, 0))\n    while queue:\n        r, c, dist = queue.popleft()\n        for dr, dc in [(0,1),(0,-1),(1,0),(-1,0)]:\n            nr, nc = r + dr, c + dc\n            if 0 <= nr < len(grid) and 0 <= nc < len(grid[0]) and grid[nr][nc] == 1:\n                grid[nr][nc] = dist + 1\n                queue.append((nr, nc, dist + 1))","timeComplexity":"O(V + E)","updatedAt":{},"whenToUse":"Shortest path, level-order traversal, minimum steps to reach target, word ladder, 0-1 BFS."},{"_id":"69a64d17ff996647cc9a6be8","name":"DFS","category":"Graph / Tree","description":"Depth-first search goes as deep as possible before backtracking. Use recursion or an explicit stack.","spaceComplexity":"O(V)","tags":["graph","tree","recursion"],"template":"# Recursive DFS on graph\ndef dfs(graph, node, visited=None):\n    if visited is None:\n        visited = set()\n    visited.add(node)\n    # process node\n    for neighbor in graph[node]:\n        if neighbor not in visited:\n            dfs(graph, neighbor, visited)\n    return visited\n\n# Iterative DFS (explicit stack)\ndef dfs_iterative(graph, start):\n    visited, stack = set(), [start]\n    while stack:\n        node = stack.pop()\n        if node not in visited:\n            visited.add(node)\n            # process node\n            stack.extend(graph[node])\n    return visited\n\n# Tree in-order traversal (iterative)\ndef inorder(root):\n    result, stack, curr = [], [], root\n    while curr or stack:\n        while curr:\n            stack.append(curr)\n            curr = curr.left\n        curr = stack.pop()\n        result.append(curr.val)\n        curr = curr.right\n    return result","timeComplexity":"O(V + E)","updatedAt":{},"whenToUse":"Connected components, path existence, cycles (directed/undirected), topological sort, tree problems."},{"_id":"69a64d17ff996647cc9a6be9","name":"Backtracking","category":"Recursion","description":"Explore all possibilities by building a candidate solution and abandoning it (\"backtrack\") as soon as it is determined it cannot lead to a valid solution.","spaceComplexity":"O(n) recursion depth","tags":["recursion","permutation","subset"],"template":"def backtrack(result, current, choices, start=0):\n    # Base case, valid complete solution\n    if is_complete(current):\n        result.append(list(current))\n        return\n\n    for i in range(start, len(choices)):\n        # Pruning\n        if not is_valid(current, choices[i]):\n            continue\n        # Choose\n        current.append(choices[i])\n        # Explore\n        backtrack(result, current, choices, i + 1)  # i+1 to avoid reuse; i to allow reuse\n        # Unchoose (backtrack)\n        current.pop()\n\n# Subsets\ndef subsets(nums):\n    result = []\n    def bt(start, curr):\n        result.append(list(curr))\n        for i in range(start, len(nums)):\n            bt(i + 1, curr + [nums[i]])\n    bt(0, [])\n    return result\n\n# Permutations\ndef permutations(nums):\n    result = []\n    def bt(curr, remaining):\n        if not remaining:\n            result.append(curr[:])\n        for i in range(len(remaining)):\n            bt(curr + [remaining[i]], remaining[:i] + remaining[i+1:])\n    bt([], nums)\n    return result","timeComplexity":"O(n! or 2^n), problem-specific","updatedAt":{},"whenToUse":"Permutations, combinations, subsets, N-queens, Sudoku, word search."},{"_id":"69a64d17ff996647cc9a6bea","name":"Dynamic Programming (Memoization)","category":"DP","description":"Top-down DP: recursively solve subproblems and cache results to avoid recomputation.","spaceComplexity":"O(states)","tags":["dp","memoization","recursion"],"template":"from functools import lru_cache\n\n# Template with lru_cache\ndef solve(n):\n    @lru_cache(maxsize=None)\n    def dp(i, j, ...):\n        # Base cases\n        if i < 0 or j < 0:\n            return 0\n        # Recurrence\n        return max(dp(i-1, j, ...), dp(i, j-1, ...))\n\n    return dp(n, m)\n\n# Manual memo dict\ndef solve_manual(n, memo={}):\n    if n in memo:\n        return memo[n]\n    if n <= 1:\n        return n\n    memo[n] = solve_manual(n-1, memo) + solve_manual(n-2, memo)\n    return memo[n]\n\n# Coin change (min coins)\ndef coin_change(coins, amount):\n    @lru_cache(maxsize=None)\n    def dp(rem):\n        if rem == 0: return 0\n        if rem < 0:  return float('inf')\n        return 1 + min(dp(rem - c) for c in coins)\n    res = dp(amount)\n    return res if res < float('inf') else -1","timeComplexity":"O(states × transition cost)","updatedAt":{},"whenToUse":"Overlapping subproblems + optimal substructure: Fibonacci, coin change, knapsack, LCS, edit distance."},{"_id":"69a64d17ff996647cc9a6beb","name":"Dynamic Programming (Tabulation)","category":"DP","description":"Bottom-up DP: fill a table iteratively from base cases up to the answer.","spaceComplexity":"O(n) or O(1) with rolling array","tags":["dp","tabulation","bottom-up"],"template":"# 1D DP\ndef dp_1d(n):\n    dp = [0] * (n + 1)\n    dp[0], dp[1] = 0, 1        # base cases\n    for i in range(2, n + 1):\n        dp[i] = dp[i-1] + dp[i-2]\n    return dp[n]\n\n# 2D DP (e.g. longest common subsequence)\ndef lcs(s1, s2):\n    m, n = len(s1), len(s2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if s1[i-1] == s2[j-1]:\n                dp[i][j] = dp[i-1][j-1] + 1\n            else:\n                dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n    return dp[m][n]\n\n# 0-1 Knapsack\ndef knapsack(weights, values, capacity):\n    n = len(weights)\n    dp = [0] * (capacity + 1)\n    for i in range(n):\n        for w in range(capacity, weights[i] - 1, -1):   # reverse to avoid reuse\n            dp[w] = max(dp[w], dp[w - weights[i]] + values[i])\n    return dp[capacity]","timeComplexity":"O(n) or O(n²) depending on dimensions","updatedAt":{},"whenToUse":"Same as memoization but avoids recursion stack overflow and is often faster in practice."},{"_id":"69a64d17ff996647cc9a6bec","name":"Monotonic Stack","category":"Stack","description":"A stack that is always sorted (increasing or decreasing). Pop when invariant is broken, capturing next greater/smaller element.","spaceComplexity":"O(n)","tags":["stack","array"],"template":"# Next Greater Element (monotonic decreasing stack)\ndef next_greater(arr):\n    n = len(arr)\n    result = [-1] * n\n    stack = []  # stores indices\n    for i, val in enumerate(arr):\n        while stack and arr[stack[-1]] < val:\n            idx = stack.pop()\n            result[idx] = val\n        stack.append(i)\n    return result\n\n# Next Smaller Element (monotonic increasing stack)\ndef next_smaller(arr):\n    result = [-1] * len(arr)\n    stack = []\n    for i, val in enumerate(arr):\n        while stack and arr[stack[-1]] > val:\n            idx = stack.pop()\n            result[idx] = val\n        stack.append(i)\n    return result\n\n# Largest Rectangle in Histogram\ndef largest_rect(heights):\n    stack, max_area = [], 0\n    heights.append(0)  # sentinel\n    for i, h in enumerate(heights):\n        while stack and heights[stack[-1]] > h:\n            height = heights[stack.pop()]\n            width = i if not stack else i - stack[-1] - 1\n            max_area = max(max_area, height * width)\n        stack.append(i)\n    return max_area","timeComplexity":"O(n), each element pushed/popped once","updatedAt":{},"whenToUse":"Next greater element, next smaller element, largest rectangle in histogram, trapping rain water, daily temperatures."},{"_id":"69a64d17ff996647cc9a6bed","name":"Heap / Priority Queue","category":"Heap","description":"Python's heapq is a min-heap. Negate values for max-heap. Use for top-K, schedule by priority, merge K sorted lists.","spaceComplexity":"O(k)","tags":["heap","priority-queue","greedy"],"template":"import heapq\n\n# Min-heap basics\nheap = []\nheapq.heappush(heap, 3)\nheapq.heappush(heap, 1)\ntop = heapq.heappop(heap)   # returns 1\npeek = heap[0]              # peek min\n\n# Max-heap: negate values\nheapq.heappush(heap, -val)\nmax_val = -heapq.heappop(heap)\n\n# Top-K largest (min-heap of size k)\ndef top_k_largest(nums, k):\n    heap = []\n    for num in nums:\n        heapq.heappush(heap, num)\n        if len(heap) > k:\n            heapq.heappop(heap)\n    return list(heap)\n\n# K-th largest\nimport heapq\ndef kth_largest(nums, k):\n    return heapq.nlargest(k, nums)[-1]\n\n# Merge K sorted lists\ndef merge_k_sorted(lists):\n    heap, result = [], []\n    for i, lst in enumerate(lists):\n        if lst:\n            heapq.heappush(heap, (lst[0], i, 0))\n    while heap:\n        val, i, j = heapq.heappop(heap)\n        result.append(val)\n        if j + 1 < len(lists[i]):\n            heapq.heappush(heap, (lists[i][j+1], i, j+1))\n    return result","timeComplexity":"O(n log k) for top-K","updatedAt":{},"whenToUse":"Top-K elements, K-th largest, merge K sorted lists, Dijkstra, task scheduling, median of stream."},{"_id":"69a64d17ff996647cc9a6bee","name":"Union Find (DSU)","category":"Graph","description":"Disjoint Set Union supports near-O(1) find and union with path compression + union by rank.","spaceComplexity":"O(n)","tags":["graph","connected-components"],"template":"class UnionFind:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n        self.components = n\n\n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])  # path compression\n        return self.parent[x]\n\n    def union(self, x, y):\n        px, py = self.find(x), self.find(y)\n        if px == py:\n            return False\n        # Union by rank\n        if self.rank[px] < self.rank[py]:\n            px, py = py, px\n        self.parent[py] = px\n        if self.rank[px] == self.rank[py]:\n            self.rank[px] += 1\n        self.components -= 1\n        return True\n\n    def connected(self, x, y):\n        return self.find(x) == self.find(y)\n\n# Usage\nuf = UnionFind(n)\nuf.union(0, 1)\nprint(uf.connected(0, 1))  # True\nprint(uf.components)       # number of distinct groups","timeComplexity":"O(α(n)) per operation ≈ O(1)","updatedAt":{},"whenToUse":"Connected components, detect cycle in undirected graph, number of islands (dynamic), minimum spanning tree (Kruskal)."},{"_id":"69a64d17ff996647cc9a6bef","name":"Trie","category":"String","description":"A prefix tree for efficient string insert, search, and prefix queries.","spaceComplexity":"O(n × m)","tags":["string","tree","prefix"],"template":"class TrieNode:\n    def __init__(self):\n        self.children = {}\n        self.is_end = False\n\nclass Trie:\n    def __init__(self):\n        self.root = TrieNode()\n\n    def insert(self, word):\n        node = self.root\n        for ch in word:\n            if ch not in node.children:\n                node.children[ch] = TrieNode()\n            node = node.children[ch]\n        node.is_end = True\n\n    def search(self, word):\n        node = self.root\n        for ch in word:\n            if ch not in node.children:\n                return False\n            node = node.children[ch]\n        return node.is_end\n\n    def starts_with(self, prefix):\n        node = self.root\n        for ch in prefix:\n            if ch not in node.children:\n                return False\n            node = node.children[ch]\n        return True\n\n    def delete(self, word):\n        def _delete(node, word, i):\n            if i == len(word):\n                node.is_end = False\n                return len(node.children) == 0\n            ch = word[i]\n            if ch not in node.children:\n                return False\n            if _delete(node.children[ch], word, i + 1):\n                del node.children[ch]\n                return not node.is_end and len(node.children) == 0\n        _delete(self.root, word, 0)","timeComplexity":"O(m) per operation where m = word length","updatedAt":{},"whenToUse":"Word search, autocomplete, prefix matching, word dictionary, boggle, longest common prefix."},{"_id":"69a64d17ff996647cc9a6bf0","name":"Topological Sort","category":"Graph","description":"Linear ordering of vertices in a DAG such that for every edge (u→v), u comes before v. Kahn's algorithm uses BFS with in-degree counts.","spaceComplexity":"O(V)","tags":["graph","dag","ordering"],"template":"from collections import deque\n\n# Kahn's Algorithm (BFS-based)\ndef topo_sort_kahn(n, edges):\n    graph = [[] for _ in range(n)]\n    in_degree = [0] * n\n    for u, v in edges:\n        graph[u].append(v)\n        in_degree[v] += 1\n\n    queue = deque(i for i in range(n) if in_degree[i] == 0)\n    order = []\n    while queue:\n        node = queue.popleft()\n        order.append(node)\n        for neighbor in graph[node]:\n            in_degree[neighbor] -= 1\n            if in_degree[neighbor] == 0:\n                queue.append(neighbor)\n\n    return order if len(order) == n else []  # empty → cycle exists\n\n# DFS-based (postorder)\ndef topo_sort_dfs(n, edges):\n    graph = [[] for _ in range(n)]\n    for u, v in edges:\n        graph[u].append(v)\n\n    visited, result = [0] * n, []  # 0=unvisited, 1=visiting, 2=done\n    def dfs(node):\n        if visited[node] == 1: return False  # cycle\n        if visited[node] == 2: return True\n        visited[node] = 1\n        for nb in graph[node]:\n            if not dfs(nb): return False\n        visited[node] = 2\n        result.append(node)\n        return True\n\n    for i in range(n):\n        if visited[i] == 0 and not dfs(i):\n            return []  # cycle\n    return result[::-1]","timeComplexity":"O(V + E)","updatedAt":{},"whenToUse":"Course schedule, build order, dependency resolution, detect cycle in directed graph."},{"_id":"69a64d17ff996647cc9a6bf1","name":"Dijkstra","category":"Graph","description":"Greedy shortest path algorithm for weighted graphs with non-negative edges. Uses a min-heap.","spaceComplexity":"O(V)","tags":["graph","shortest-path","weighted"],"template":"import heapq\n\ndef dijkstra(graph, start, n):\n    # graph: {node: [(neighbor, weight), ...]}\n    dist = [float('inf')] * n\n    dist[start] = 0\n    heap = [(0, start)]   # (cost, node)\n\n    while heap:\n        d, u = heapq.heappop(heap)\n        if d > dist[u]:\n            continue       # stale entry\n        for v, w in graph[u]:\n            if dist[u] + w < dist[v]:\n                dist[v] = dist[u] + w\n                heapq.heappush(heap, (dist[v], v))\n\n    return dist  # dist[i] = shortest distance from start to i\n\n# With path reconstruction\ndef dijkstra_path(graph, start, end, n):\n    dist = [float('inf')] * n\n    dist[start] = 0\n    prev = [-1] * n\n    heap = [(0, start)]\n\n    while heap:\n        d, u = heapq.heappop(heap)\n        if d > dist[u]: continue\n        for v, w in graph[u]:\n            if dist[u] + w < dist[v]:\n                dist[v] = dist[u] + w\n                prev[v] = u\n                heapq.heappush(heap, (dist[v], v))\n\n    # Reconstruct path\n    path, node = [], end\n    while node != -1:\n        path.append(node)\n        node = prev[node]\n    return path[::-1]","timeComplexity":"O((V + E) log V)","updatedAt":{},"whenToUse":"Shortest path in weighted graph, network delay time, cheapest flights within K stops (modified)."},{"_id":"69a64d17ff996647cc9a6bf2","name":"Prefix Sum","category":"Array / String","description":"Precompute cumulative sums for O(1) range sum queries.","spaceComplexity":"O(n)","tags":["array","precomputation"],"template":"# 1D Prefix Sum\ndef build_prefix(arr):\n    prefix = [0] * (len(arr) + 1)\n    for i, val in enumerate(arr):\n        prefix[i + 1] = prefix[i] + val\n    return prefix\n\ndef range_sum(prefix, l, r):  # inclusive [l, r], 0-indexed\n    return prefix[r + 1] - prefix[l]\n\n# Subarray sum equals k (HashMap approach)\nfrom collections import defaultdict\ndef subarray_sum_k(nums, k):\n    count, curr, freq = 0, 0, defaultdict(int)\n    freq[0] = 1\n    for num in nums:\n        curr += num\n        count += freq[curr - k]\n        freq[curr] += 1\n    return count\n\n# 2D Prefix Sum\ndef build_2d_prefix(mat):\n    m, n = len(mat), len(mat[0])\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for r in range(1, m + 1):\n        for c in range(1, n + 1):\n            dp[r][c] = mat[r-1][c-1] + dp[r-1][c] + dp[r][c-1] - dp[r-1][c-1]\n    return dp\n\ndef query_2d(dp, r1, c1, r2, c2):  # inclusive, 0-indexed\n    return dp[r2+1][c2+1] - dp[r1][c2+1] - dp[r2+1][c1] + dp[r1][c1]","timeComplexity":"O(n) build, O(1) query","updatedAt":{},"whenToUse":"Subarray sum equals k, range sum queries, 2D range sums, product except self."},{"_id":"69a64d17ff996647cc9a6bf3","name":"Merge Intervals","category":"Array / String","description":"Sort by start time, then merge overlapping intervals by comparing current end with next start.","spaceComplexity":"O(n)","tags":["array","sorting","intervals"],"template":"# Merge overlapping intervals\ndef merge(intervals):\n    intervals.sort(key=lambda x: x[0])\n    merged = [intervals[0]]\n    for start, end in intervals[1:]:\n        if start <= merged[-1][1]:\n            merged[-1][1] = max(merged[-1][1], end)\n        else:\n            merged.append([start, end])\n    return merged\n\n# Insert interval\ndef insert(intervals, new_interval):\n    result = []\n    i, n = 0, len(intervals)\n    # Add all non-overlapping before new_interval\n    while i < n and intervals[i][1] < new_interval[0]:\n        result.append(intervals[i]); i += 1\n    # Merge overlapping\n    while i < n and intervals[i][0] <= new_interval[1]:\n        new_interval[0] = min(new_interval[0], intervals[i][0])\n        new_interval[1] = max(new_interval[1], intervals[i][1])\n        i += 1\n    result.append(new_interval)\n    result.extend(intervals[i:])\n    return result\n\n# Minimum meeting rooms (sweep line)\nimport heapq\ndef min_meeting_rooms(intervals):\n    intervals.sort()\n    heap = []   # end times\n    for start, end in intervals:\n        if heap and heap[0] <= start:\n            heapq.heapreplace(heap, end)\n        else:\n            heapq.heappush(heap, end)\n    return len(heap)","timeComplexity":"O(n log n)","updatedAt":{},"whenToUse":"Merge intervals, insert interval, meeting rooms, minimum meeting rooms (sweep line)."},{"_id":"69a64d17ff996647cc9a6bf4","name":"Matrix DFS/BFS (Islands)","category":"Graph / Tree","description":"Treat a 2D grid as a graph. DFS or BFS from each unvisited cell, marking visited to avoid cycles.","spaceComplexity":"O(m × n)","tags":["graph","grid","dfs","bfs"],"template":"DIRS = [(0,1),(0,-1),(1,0),(-1,0)]\n\n# DFS flood-fill\ndef dfs(grid, r, c, visited=None):\n    if visited is None: visited = set()\n    if (r < 0 or r >= len(grid) or c < 0 or c >= len(grid[0])\n            or grid[r][c] == '0' or (r, c) in visited):\n        return\n    visited.add((r, c))\n    for dr, dc in DIRS:\n        dfs(grid, r + dr, c + dc, visited)\n\ndef num_islands(grid):\n    count = 0\n    for r in range(len(grid)):\n        for c in range(len(grid[0])):\n            if grid[r][c] == '1':\n                dfs(grid, r, c)  # marks visited by flipping to '0'\n                count += 1\n    return count\n\n# BFS shortest path in grid\nfrom collections import deque\ndef shortest_path(grid, start, end):\n    queue = deque([(start, 0)])\n    visited = {start}\n    while queue:\n        (r, c), dist = queue.popleft()\n        if (r, c) == end:\n            return dist\n        for dr, dc in DIRS:\n            nr, nc = r + dr, c + dc\n            if (0 <= nr < len(grid) and 0 <= nc < len(grid[0])\n                    and grid[nr][nc] != '#' and (nr, nc) not in visited):\n                visited.add((nr, nc))\n                queue.append(((nr, nc), dist + 1))\n    return -1","timeComplexity":"O(m × n)","updatedAt":{},"whenToUse":"Number of islands, flood fill, max area of island, surrounded regions, shortest path in grid."},{"_id":"69a64d18ff996647cc9a6bf5","name":"HashMap / Frequency Counter","category":"Array / String","description":"Use a hash map to count frequencies or store last seen indices. Eliminates the need for nested loops.","spaceComplexity":"O(n)","tags":["hash-map","array","string"],"template":"from collections import Counter, defaultdict\n\n# Two Sum (index lookup)\ndef two_sum(nums, target):\n    seen = {}\n    for i, num in enumerate(nums):\n        complement = target - num\n        if complement in seen:\n            return [seen[complement], i]\n        seen[num] = i\n    return []\n\n# Frequency counter (anagram check)\ndef is_anagram(s, t):\n    return Counter(s) == Counter(t)\n\n# Longest substring without repeating characters\ndef length_of_longest_substring(s):\n    char_idx = {}\n    left = result = 0\n    for right, ch in enumerate(s):\n        if ch in char_idx and char_idx[ch] >= left:\n            left = char_idx[ch] + 1\n        char_idx[ch] = right\n        result = max(result, right - left + 1)\n    return result\n\n# Group anagrams\ndef group_anagrams(strs):\n    groups = defaultdict(list)\n    for s in strs:\n        groups[tuple(sorted(s))].append(s)\n    return list(groups.values())","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Two sum, group anagrams, first unique character, longest substring without repeat, valid anagram."},{"_id":"69a64d18ff996647cc9a6bf6","name":"LRU Cache","category":"Design","description":"Least Recently Used cache evicts the least recently accessed item. Implemented with a doubly linked list + hashmap for O(1) get & put.","spaceComplexity":"O(capacity)","tags":["design","linked-list","hash-map"],"template":"from collections import OrderedDict\n\n# Python shortcut using OrderedDict\nclass LRUCache:\n    def __init__(self, capacity):\n        self.cap = capacity\n        self.cache = OrderedDict()\n\n    def get(self, key):\n        if key not in self.cache:\n            return -1\n        self.cache.move_to_end(key)\n        return self.cache[key]\n\n    def put(self, key, value):\n        if key in self.cache:\n            self.cache.move_to_end(key)\n        self.cache[key] = value\n        if len(self.cache) > self.cap:\n            self.cache.popitem(last=False)\n\n# From-scratch implementation (doubly linked list + dict)\nclass Node:\n    def __init__(self, k=0, v=0):\n        self.k, self.v = k, v\n        self.prev = self.next = None\n\nclass LRUCacheScratch:\n    def __init__(self, capacity):\n        self.cap = capacity\n        self.map = {}\n        self.head = Node()   # dummy head (LRU end)\n        self.tail = Node()   # dummy tail (MRU end)\n        self.head.next, self.tail.prev = self.tail, self.head\n\n    def _remove(self, node):\n        node.prev.next, node.next.prev = node.next, node.prev\n\n    def _insert_tail(self, node):\n        node.prev, node.next = self.tail.prev, self.tail\n        self.tail.prev.next = self.tail.prev = node\n\n    def get(self, key):\n        if key not in self.map: return -1\n        self._remove(self.map[key])\n        self._insert_tail(self.map[key])\n        return self.map[key].v\n\n    def put(self, key, value):\n        if key in self.map: self._remove(self.map[key])\n        self.map[key] = Node(key, value)\n        self._insert_tail(self.map[key])\n        if len(self.map) > self.cap:\n            lru = self.head.next\n            self._remove(lru)\n            del self.map[lru.k]","timeComplexity":"O(1) get and put","updatedAt":{},"whenToUse":"System design (caching), interview implementation question."},{"_id":"69a64d18ff996647cc9a6bf7","name":"Cyclic Sort","category":"Array / String","description":"Place each number at its correct index by swapping. Works on arrays with values in range [1..n] or [0..n-1].","spaceComplexity":"O(1)","tags":["array","in-place","sorting"],"template":"# Cyclic sort, numbers 1..n\ndef cyclic_sort(nums):\n    i = 0\n    while i < len(nums):\n        j = nums[i] - 1      # correct 0-indexed position\n        if nums[i] != nums[j]:\n            nums[i], nums[j] = nums[j], nums[i]\n        else:\n            i += 1\n    return nums\n\n# Find the missing number (1..n)\ndef find_missing(nums):\n    cyclic_sort(nums)\n    for i, num in enumerate(nums):\n        if num != i + 1:\n            return i + 1\n    return len(nums) + 1\n\n# Find the duplicate number\ndef find_duplicate(nums):\n    i = 0\n    while i < len(nums):\n        j = nums[i] - 1\n        if nums[i] != i + 1:\n            if nums[i] != nums[j]:\n                nums[i], nums[j] = nums[j], nums[i]\n            else:\n                return nums[i]\n        else:\n            i += 1\n    return -1","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Find missing number, find duplicate, find all duplicates, smallest missing positive."},{"_id":"69a64ed2ff996647cc9a6c02","name":"Bit Manipulation","category":"Bit Manipulation","description":"Operate directly on binary representations. XOR, AND, OR, shifts. Many tricks arise from bit-level properties.","spaceComplexity":"O(1)","tags":["bits","xor","bitmask"],"template":"# XOR tricks\n# a ^ a = 0, a ^ 0 = a\ndef single_number(nums):\n    return reduce(xor, nums)       # every pair cancels\n\ndef missing_number(nums):          # nums has [0..n] with one missing\n    return reduce(xor, range(len(nums)+1)) ^ reduce(xor, nums)\n\n# Brian Kernighan, count set bits in O(# of set bits)\ndef count_bits(n):\n    count = 0\n    while n:\n        n &= n - 1    # clears lowest set bit\n        count += 1\n    return count\n\n# Common checks\ndef is_power_of_two(n): return n > 0 and (n & (n - 1)) == 0\ndef get_bit(n, i):  return (n >> i) & 1\ndef set_bit(n, i):  return n | (1 << i)\ndef clear_bit(n,i): return n & ~(1 << i)\ndef flip_bit(n, i): return n ^ (1 << i)\ndef lowest_set_bit(n): return n & (-n)\n\n# Bitmask DP (enumerate all subsets)\ndef subset_sum(nums, target):\n    n = len(nums)\n    for mask in range(1 << n):    # 2^n subsets\n        total = sum(nums[i] for i in range(n) if mask >> i & 1)\n        if total == target:\n            return [nums[i] for i in range(n) if mask >> i & 1]\n\n# Enumeration of all subsets of a bitmask (Gosper's hack)\ndef all_subsets_of_mask(mask):\n    sub = mask\n    while sub:\n        yield sub\n        sub = (sub - 1) & mask","timeComplexity":"O(1) for most tricks, O(n) for array scans","updatedAt":{},"whenToUse":"Single number, missing number, subsets (bitmask DP), count bits, power of 2, swap without temp, gray code."},{"_id":"69a64ed2ff996647cc9a6c03","name":"GCD, LCM & Euclidean Algorithm","category":"Math / Number Theory","description":"Euclid's algorithm computes GCD in O(log min(a,b)). Key insight: gcd(a,b) = gcd(b, a mod b).","spaceComplexity":"O(1)","tags":["math","number-theory"],"template":"import math\n\n# Python built-in (use this in interviews)\ng = math.gcd(a, b)\nl = math.lcm(a, b)   # Python 3.9+\n\n# Manual implementations\ndef gcd(a, b):\n    while b:\n        a, b = b, a % b\n    return a\n\ndef lcm(a, b):\n    return a * b // gcd(a, b)\n\n# Extended Euclidean, finds x,y such that ax + by = gcd(a,b)\ndef extended_gcd(a, b):\n    if b == 0:\n        return a, 1, 0\n    g, x1, y1 = extended_gcd(b, a % b)\n    return g, y1, x1 - (a // b) * y1\n\n# Modular inverse (when gcd(a, m) = 1)\ndef mod_inverse(a, m):\n    g, x, _ = extended_gcd(a, m)\n    if g != 1:\n        raise ValueError(\"Inverse doesn't exist\")\n    return x % m\n\n# Check coprime\ndef coprime(a, b): return gcd(a, b) == 1","timeComplexity":"O(log min(a, b))","updatedAt":{},"whenToUse":"Simplify fractions, find LCM, solve Diophantine equations, check if two numbers are coprime."},{"_id":"69a64ed2ff996647cc9a6c04","name":"Sieve of Eratosthenes","category":"Math / Number Theory","description":"Find all primes up to N in O(N log log N). Classic preprocessing for problems involving primes.","spaceComplexity":"O(N)","tags":["math","primes","sieve"],"template":"def sieve(n):\n    is_prime = [True] * (n + 1)\n    is_prime[0] = is_prime[1] = False\n    for i in range(2, int(n**0.5) + 1):\n        if is_prime[i]:\n            for j in range(i*i, n+1, i):\n                is_prime[j] = False\n    return [i for i in range(2, n+1) if is_prime[i]]\n\n# Smallest prime factor sieve (enables fast factorization)\ndef spf_sieve(n):\n    spf = list(range(n + 1))\n    for i in range(2, int(n**0.5) + 1):\n        if spf[i] == i:                  # i is prime\n            for j in range(i*i, n+1, i):\n                if spf[j] == j:\n                    spf[j] = i\n    return spf\n\ndef factorize(n, spf):\n    factors = []\n    while n > 1:\n        factors.append(spf[n])\n        n //= spf[n]\n    return factors\n\n# Single primality check (Miller-Rabin for large n)\ndef is_prime(n):\n    if n < 2: return False\n    if n < 4: return True\n    if n % 2 == 0 or n % 3 == 0: return False\n    i = 5\n    while i * i <= n:\n        if n % i == 0 or n % (i+2) == 0:\n            return False\n        i += 6\n    return True","timeComplexity":"O(N log log N)","updatedAt":{},"whenToUse":"Count primes, prime factorisation, check primality for many numbers, Goldbach, twin primes."},{"_id":"69a64ed2ff996647cc9a6c05","name":"Modular Arithmetic & Fast Exponentiation","category":"Math / Number Theory","description":"Compute a^b mod m in O(log b) using repeated squaring. Essential for combinatorics, hashing, and cryptography problems.","spaceComplexity":"O(1)","tags":["math","modular","exponentiation"],"template":"MOD = 10**9 + 7\n\n# Fast power (built-in in Python: pow(a, b, mod))\ndef fast_pow(base, exp, mod):\n    result = 1\n    base %= mod\n    while exp > 0:\n        if exp & 1:\n            result = result * base % mod\n        base = base * base % mod\n        exp >>= 1\n    return result\n\n# nCr mod prime (Fermat's little theorem: inv = pow(x, p-2, p))\ndef precompute_factorials(n, mod):\n    fact = [1] * (n + 1)\n    inv_fact = [1] * (n + 1)\n    for i in range(1, n + 1):\n        fact[i] = fact[i-1] * i % mod\n    inv_fact[n] = pow(fact[n], mod - 2, mod)\n    for i in range(n - 1, -1, -1):\n        inv_fact[i] = inv_fact[i+1] * (i+1) % mod\n    return fact, inv_fact\n\ndef nCr(n, r, fact, inv_fact, mod):\n    if r < 0 or r > n: return 0\n    return fact[n] * inv_fact[r] % mod * inv_fact[n-r] % mod\n\n# Chinese Remainder Theorem (for coprime moduli)\ndef crt(remainders, moduli):\n    M = 1\n    for m in moduli: M *= m\n    x = 0\n    for r, m in zip(remainders, moduli):\n        Mi = M // m\n        x += r * Mi * pow(Mi, -1, m)   # Python 3.8+ pow with -1 for modular inverse\n    return x % M","timeComplexity":"O(log b)","updatedAt":{},"whenToUse":"Large powers mod prime, combinations mod p (nCr), Fermat's little theorem, modular inverse."},{"_id":"69a64ed2ff996647cc9a6c06","name":"KMP, String Matching","category":"Advanced String","description":"Knuth-Morris-Pratt finds all occurrences of a pattern in a text in O(n+m) by precomputing a failure function that avoids redundant comparisons.","spaceComplexity":"O(m)","tags":["string","pattern-matching"],"template":"def kmp_search(text, pattern):\n    n, m = len(text), len(pattern)\n    if m == 0: return [0]\n\n    # Build failure function (LPS array)\n    lps = [0] * m\n    length, i = 0, 1\n    while i < m:\n        if pattern[i] == pattern[length]:\n            length += 1\n            lps[i] = length\n            i += 1\n        elif length:\n            length = lps[length - 1]\n        else:\n            lps[i] = 0\n            i += 1\n\n    # Search\n    matches = []\n    i = j = 0\n    while i < n:\n        if text[i] == pattern[j]:\n            i += 1; j += 1\n        if j == m:\n            matches.append(i - j)\n            j = lps[j - 1]\n        elif i < n and text[i] != pattern[j]:\n            j = lps[j - 1] if j else (i := i + 1) or 0\n    return matches\n\n# Is s2 a rotation of s1?\ndef is_rotation(s1, s2):\n    if len(s1) != len(s2): return False\n    return bool(kmp_search(s1 + s1, s2))","timeComplexity":"O(n + m)","updatedAt":{},"whenToUse":"String search in O(n+m), find pattern in text, check if s2 is rotation of s1 (search in s1+s1), strStr()."},{"_id":"69a64ed2ff996647cc9a6c07","name":"Rabin-Karp Rolling Hash","category":"Advanced String","description":"Hash the pattern and slide a window over the text, recomputing the hash in O(1) per step via a rolling formula. Average O(n+m).","spaceComplexity":"O(1)","tags":["string","hashing","rolling-hash"],"template":"def rabin_karp(text, pattern):\n    n, m = len(text), len(pattern)\n    BASE, MOD = 31, 10**9 + 7\n\n    def poly_hash(s, length):\n        h = 0\n        for i in range(length):\n            h = (h * BASE + ord(s[i]) - ord('a') + 1) % MOD\n        return h\n\n    ph = poly_hash(pattern, m)\n    th = poly_hash(text, m)\n    power = pow(BASE, m - 1, MOD)   # BASE^(m-1) for removal\n    matches = []\n    if th == ph and text[:m] == pattern: matches.append(0)\n\n    for i in range(1, n - m + 1):\n        # Roll: remove leftmost char, add new rightmost char\n        th = (th - (ord(text[i-1]) - ord('a') + 1) * power) % MOD\n        th = (th * BASE + ord(text[i+m-1]) - ord('a') + 1) % MOD\n        th %= MOD\n        if th == ph and text[i:i+m] == pattern:\n            matches.append(i)\n    return matches\n\n# Longest Duplicate Substring (binary search + rolling hash)\ndef longest_dup_sub(s):\n    def has_dup(length):\n        BASE, MOD = 31, 2**61 - 1\n        power = pow(BASE, length, MOD)\n        h = 0\n        for ch in s[:length]: h = (h * BASE + ord(ch)) % MOD\n        seen = {h}\n        for i in range(length, len(s)):\n            h = (h * BASE + ord(s[i]) - (ord(s[i-length]) * power)) % MOD\n            if h in seen: return True\n            seen.add(h)\n        return False\n    lo, hi, ans = 0, len(s), \"\"\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if has_dup(mid): lo = mid + 1\n        else: hi = mid - 1\n    return ans","timeComplexity":"O(n + m) average, O(nm) worst","updatedAt":{},"whenToUse":"Find pattern in text, find duplicate substrings, longest duplicate substring (binary search + rolling hash), 2D pattern matching."},{"_id":"69a64ed2ff996647cc9a6c08","name":"Manacher's Algorithm","category":"Advanced String","description":"Finds the longest palindromic substring in O(n) by exploiting mirror symmetry of palindromes already found.","spaceComplexity":"O(n)","tags":["string","palindrome"],"template":"def manacher(s):\n    # Transform s to handle even-length palindromes\n    # \"abc\" → \"#a#b#c#\"\n    t = '#' + '#'.join(s) + '#'\n    n = len(t)\n    p = [0] * n    # p[i] = radius of palindrome centered at i\n    center = right = 0\n\n    for i in range(n):\n        if i < right:\n            mirror = 2 * center - i\n            p[i] = min(right - i, p[mirror])\n        # Try to expand\n        while (i - p[i] - 1 >= 0 and i + p[i] + 1 < n\n               and t[i - p[i] - 1] == t[i + p[i] + 1]):\n            p[i] += 1\n        # Update center and right boundary\n        if i + p[i] > right:\n            center, right = i, i + p[i]\n\n    # Longest palindrome\n    max_len, center_idx = max((v, i) for i, v in enumerate(p))\n    start = (center_idx - max_len) // 2\n    return s[start: start + max_len]\n\ndef count_palindromes(s):\n    t = '#' + '#'.join(s) + '#'\n    n, p = len(t), [0] * len(t)\n    center = right = 0\n    for i in range(n):\n        if i < right: p[i] = min(right-i, p[2*center-i])\n        while i-p[i]-1>=0 and i+p[i]+1<n and t[i-p[i]-1]==t[i+p[i]+1]: p[i]+=1\n        if i+p[i]>right: center, right = i, i+p[i]\n    return sum(v // 2 + 1 for v in p)","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Longest palindromic substring, count palindromic substrings in O(n), palindrome partitioning."},{"_id":"69a64ed2ff996647cc9a6c09","name":"Z-Algorithm","category":"Advanced String","description":"Computes Z[i] = length of the longest substring starting at i that is also a prefix of s. Equivalent power to KMP.","spaceComplexity":"O(n)","tags":["string","pattern-matching","prefix"],"template":"def z_function(s):\n    n = len(s)\n    z = [0] * n\n    z[0] = n\n    l = r = 0\n    for i in range(1, n):\n        if i < r:\n            z[i] = min(r - i, z[i - l])\n        while i + z[i] < n and s[z[i]] == s[i + z[i]]:\n            z[i] += 1\n        if i + z[i] > r:\n            l, r = i, i + z[i]\n    return z\n\ndef z_search(text, pattern):\n    \"\"\"Find all occurrences of pattern in text.\"\"\"\n    s = pattern + '#' + text\n    z = z_function(s)\n    m = len(pattern)\n    return [i - m - 1 for i in range(m + 1, len(s)) if z[i] == m]\n\ndef smallest_period(s):\n    \"\"\"Smallest period p such that s is a repetition of s[:p].\"\"\"\n    z = z_function(s)\n    n = len(s)\n    for p in range(1, n):\n        if n % p == 0 and z[p] == n - p:\n            return p\n    return n","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Pattern matching (concatenate pattern + # + text, look for Z[i] = len(pattern)), string period detection."},{"_id":"69a64ed2ff996647cc9a6c0a","name":"Bellman-Ford","category":"Advanced Graph","description":"Shortest path for graphs with negative edges. Relax all edges V-1 times. Can detect negative cycles.","spaceComplexity":"O(V)","tags":["graph","shortest-path","negative-edges"],"template":"def bellman_ford(n, edges, src):\n    \"\"\"edges: list of (u, v, weight)\"\"\"\n    dist = [float('inf')] * n\n    dist[src] = 0\n\n    # Relax all edges V-1 times\n    for _ in range(n - 1):\n        updated = False\n        for u, v, w in edges:\n            if dist[u] != float('inf') and dist[u] + w < dist[v]:\n                dist[v] = dist[u] + w\n                updated = True\n        if not updated:\n            break     # early exit\n\n    # Check for negative cycles (one more pass)\n    for u, v, w in edges:\n        if dist[u] != float('inf') and dist[u] + w < dist[v]:\n            return None    # negative cycle exists\n\n    return dist\n\n# Cheapest flights within K stops (Bellman-Ford variant)\ndef find_cheapest_price(n, flights, src, dst, k):\n    prices = [float('inf')] * n\n    prices[src] = 0\n    for _ in range(k + 1):\n        tmp = prices[:]\n        for u, v, w in flights:\n            if prices[u] != float('inf') and prices[u] + w < tmp[v]:\n                tmp[v] = prices[u] + w\n        prices = tmp\n    return prices[dst] if prices[dst] < float('inf') else -1","timeComplexity":"O(V × E)","updatedAt":{},"whenToUse":"Negative edge weights, detect negative cycles, currency arbitrage, cheapest flights with K stops."},{"_id":"69a64ed2ff996647cc9a6c0b","name":"Floyd-Warshall","category":"Advanced Graph","description":"All-pairs shortest path via DP. dist[i][j] updated by routing through intermediate node k.","spaceComplexity":"O(V²)","tags":["graph","all-pairs","dp"],"template":"def floyd_warshall(n, edges):\n    \"\"\"Returns dist[i][j] = shortest path from i to j (or inf).\"\"\"\n    INF = float('inf')\n    dist = [[INF] * n for _ in range(n)]\n    for i in range(n):\n        dist[i][i] = 0\n    for u, v, w in edges:\n        dist[u][v] = min(dist[u][v], w)\n        # For undirected: dist[v][u] = min(dist[v][u], w)\n\n    for k in range(n):\n        for i in range(n):\n            for j in range(n):\n                if dist[i][k] + dist[k][j] < dist[i][j]:\n                    dist[i][j] = dist[i][k] + dist[k][j]\n\n    # Negative cycle check: dist[i][i] < 0 for some i\n    has_neg_cycle = any(dist[i][i] < 0 for i in range(n))\n    return dist, has_neg_cycle\n\n# Transitive closure\ndef transitive_closure(n, adj):\n    reach = [[bool(adj[i][j]) for j in range(n)] for i in range(n)]\n    for i in range(n): reach[i][i] = True\n    for k in range(n):\n        for i in range(n):\n            for j in range(n):\n                reach[i][j] |= reach[i][k] and reach[k][j]\n    return reach","timeComplexity":"O(V³)","updatedAt":{},"whenToUse":"All-pairs shortest path, detect negative cycles, transitive closure, find the diameter of a graph."},{"_id":"69a64ed2ff996647cc9a6c0c","name":"Tarjan's, Bridges & SCCs","category":"Advanced Graph","description":"Tarjan's algorithm finds bridges, articulation points, and strongly connected components (SCCs) in one DFS pass using discovery times and low-link values.","spaceComplexity":"O(V)","tags":["graph","bridges","scc","dfs"],"template":"# Find bridges (critical connections)\ndef find_bridges(n, edges):\n    graph = [[] for _ in range(n)]\n    for u, v in edges:\n        graph[u].append(v); graph[v].append(u)\n\n    disc = [-1] * n\n    low  = [0]  * n\n    bridges = []\n    timer = [0]\n\n    def dfs(u, parent):\n        disc[u] = low[u] = timer[0]; timer[0] += 1\n        for v in graph[u]:\n            if disc[v] == -1:\n                dfs(v, u)\n                low[u] = min(low[u], low[v])\n                if low[v] > disc[u]:       # bridge condition\n                    bridges.append((u, v))\n            elif v != parent:\n                low[u] = min(low[u], disc[v])\n\n    for i in range(n):\n        if disc[i] == -1: dfs(i, -1)\n    return bridges\n\n# Kosaraju's SCC (2-pass DFS on original + reversed graph)\ndef kosaraju(n, edges):\n    graph  = [[] for _ in range(n)]\n    rgraph = [[] for _ in range(n)]\n    for u, v in edges:\n        graph[u].append(v); rgraph[v].append(u)\n\n    visited, order = [False]*n, []\n    def dfs1(u):\n        visited[u] = True\n        for v in graph[u]:\n            if not visited[v]: dfs1(v)\n        order.append(u)\n\n    comp = [-1]*n\n    def dfs2(u, c):\n        comp[u] = c\n        for v in rgraph[u]:\n            if comp[v] == -1: dfs2(v, c)\n\n    for i in range(n):\n        if not visited[i]: dfs1(i)\n    c = 0\n    for u in reversed(order):\n        if comp[u] == -1: dfs2(u, c); c += 1\n    return comp, c   # component labels, number of SCCs","timeComplexity":"O(V + E)","updatedAt":{},"whenToUse":"Find bridges (critical connections), articulation points, SCCs in directed graph, 2-SAT."},{"_id":"69a64ed2ff996647cc9a6c0d","name":"Kruskal's MST","category":"Advanced Graph","description":"Minimum Spanning Tree: sort edges by weight, greedily add edges that don't form a cycle (checked via Union-Find).","spaceComplexity":"O(V)","tags":["graph","mst","greedy"],"template":"def kruskal(n, edges):\n    \"\"\"edges: [(weight, u, v)]  Returns (total_weight, mst_edges)\"\"\"\n    edges.sort()\n    parent = list(range(n))\n    rank   = [0] * n\n\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]  # path halving\n            x = parent[x]\n        return x\n\n    def union(x, y):\n        px, py = find(x), find(y)\n        if px == py: return False\n        if rank[px] < rank[py]: px, py = py, px\n        parent[py] = px\n        if rank[px] == rank[py]: rank[px] += 1\n        return True\n\n    total, mst = 0, []\n    for w, u, v in edges:\n        if union(u, v):\n            total += w\n            mst.append((u, v, w))\n            if len(mst) == n - 1:\n                break          # MST complete\n    return total, mst","timeComplexity":"O(E log E)","updatedAt":{},"whenToUse":"Minimum cost to connect all nodes, network design, clustering, Borůvka."},{"_id":"69a64ed2ff996647cc9a6c0e","name":"Segment Tree","category":"Advanced Data Structures","description":"Binary tree where each node stores aggregate of a range. Supports O(log n) point updates and range queries (sum, min, max).","spaceComplexity":"O(n)","tags":["segment-tree","range-query"],"template":"class SegTree:\n    def __init__(self, n):\n        self.n = n\n        self.tree = [0] * (4 * n)\n\n    def build(self, arr, node=1, start=0, end=None):\n        if end is None: end = self.n - 1\n        if start == end:\n            self.tree[node] = arr[start]\n        else:\n            mid = (start + end) // 2\n            self.build(arr, 2*node,   start,   mid)\n            self.build(arr, 2*node+1, mid+1, end)\n            self.tree[node] = self.tree[2*node] + self.tree[2*node+1]\n\n    def update(self, idx, val, node=1, start=0, end=None):\n        if end is None: end = self.n - 1\n        if start == end:\n            self.tree[node] = val\n        else:\n            mid = (start + end) // 2\n            if idx <= mid: self.update(idx, val, 2*node,   start, mid)\n            else:          self.update(idx, val, 2*node+1, mid+1, end)\n            self.tree[node] = self.tree[2*node] + self.tree[2*node+1]\n\n    def query(self, l, r, node=1, start=0, end=None):\n        if end is None: end = self.n - 1\n        if r < start or end < l: return 0          # out of range\n        if l <= start and end <= r: return self.tree[node]  # fully covered\n        mid = (start + end) // 2\n        return (self.query(l, r, 2*node,   start, mid) +\n                self.query(l, r, 2*node+1, mid+1, end))\n\n# Usage\nst = SegTree(len(arr))\nst.build(arr)\nst.update(i, new_val)\nresult = st.query(l, r)  # sum [l..r]","timeComplexity":"O(n) build, O(log n) query/update","updatedAt":{},"whenToUse":"Range sum/min/max queries with updates, count inversions, falling squares, rectangle area."},{"_id":"69a64ed3ff996647cc9a6c0f","name":"Fenwick Tree (BIT)","category":"Advanced Data Structures","description":"Binary Indexed Tree supports O(log n) prefix sum queries and point updates with less code than a Segment Tree.","spaceComplexity":"O(n)","tags":["bit","fenwick","prefix-sum"],"template":"class BIT:\n    def __init__(self, n):\n        self.n = n\n        self.tree = [0] * (n + 1)\n\n    def update(self, i, delta=1):\n        \"\"\"Add delta to index i (1-indexed).\"\"\"\n        while i <= self.n:\n            self.tree[i] += delta\n            i += i & (-i)      # move to parent\n\n    def query(self, i):\n        \"\"\"Prefix sum [1..i] (1-indexed).\"\"\"\n        s = 0\n        while i > 0:\n            s += self.tree[i]\n            i -= i & (-i)      # move to lower significant bit\n        return s\n\n    def range_query(self, l, r):\n        return self.query(r) - self.query(l - 1)\n\n# Count inversions using BIT\ndef count_inversions(arr):\n    compressed = {v: i+1 for i, v in enumerate(sorted(set(arr)))}\n    bit = BIT(len(arr))\n    inv = 0\n    for num in reversed(arr):           # process right to left\n        idx = compressed[num]\n        inv += bit.query(idx - 1)       # count smaller elements to the right\n        bit.update(idx)\n    return inv\n\n# 2D BIT\nclass BIT2D:\n    def __init__(self, m, n):\n        self.m, self.n = m, n\n        self.tree = [[0]*(n+1) for _ in range(m+1)]\n    def update(self, r, c, v=1):\n        i = r\n        while i <= self.m:\n            j = c\n            while j <= self.n:\n                self.tree[i][j] += v; j += j&-j\n            i += i&-i\n    def query(self, r, c):\n        s, i = 0, r\n        while i: j=c;\n        while i:\n            j = c\n            while j: s += self.tree[i][j]; j -= j&-j\n            i -= i&-i\n        return s","timeComplexity":"O(log n) per update/query, O(n log n) build","updatedAt":{},"whenToUse":"Prefix sum with updates, count of smaller numbers after self, 2D BIT for 2D range sums, inversions."},{"_id":"69a64ed3ff996647cc9a6c10","name":"Kadane's Algorithm","category":"Clever Tricks","description":"Maximum subarray sum in O(n). Extend current subarray or start fresh when running sum goes negative.","spaceComplexity":"O(1)","tags":["array","dp","greedy"],"template":"def max_subarray(nums):\n    best = curr = nums[0]\n    for x in nums[1:]:\n        curr = max(x, curr + x)\n        best = max(best, curr)\n    return best\n\n# With indices\ndef max_subarray_idx(nums):\n    best = curr = nums[0]\n    start = end = temp_start = 0\n    for i in range(1, len(nums)):\n        if nums[i] > curr + nums[i]:\n            curr = nums[i]; temp_start = i\n        else:\n            curr += nums[i]\n        if curr > best:\n            best = curr; start = temp_start; end = i\n    return best, start, end\n\n# Maximum circular subarray = max(kadane, total - min_subarray)\ndef max_circular(nums):\n    def kadane(a):\n        best = curr = a[0]\n        for x in a[1:]: curr = max(x, curr+x); best = max(best, curr)\n        return best\n    def min_sub(a):\n        worst = curr = a[0]\n        for x in a[1:]: curr = min(x, curr+x); worst = min(worst, curr)\n        return worst\n    total = sum(nums)\n    wrapped = total - min_sub(nums)\n    return max(kadane(nums), wrapped) if wrapped != 0 else kadane(nums)","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Maximum subarray (Leetcode 53), maximum circular subarray, maximum product subarray (modified)."},{"_id":"69a64ed3ff996647cc9a6c11","name":"Boyer-Moore Voting Algorithm","category":"Clever Tricks","description":"Find the majority element (appears > n/2 times) in O(n) time and O(1) space. Maintain a candidate and cancel it with different elements.","spaceComplexity":"O(1)","tags":["array","voting"],"template":"# Majority element (> n/2)\ndef majority_element(nums):\n    candidate, count = None, 0\n    for num in nums:\n        if count == 0:\n            candidate = num\n        count += 1 if num == candidate else -1\n    # Optional: verify candidate actually has majority\n    return candidate\n\n# Elements appearing > n/3 times (at most 2 candidates)\ndef majority_element_n3(nums):\n    c1, c2, cnt1, cnt2 = None, None, 0, 0\n    for num in nums:\n        if num == c1:   cnt1 += 1\n        elif num == c2: cnt2 += 1\n        elif cnt1 == 0: c1, cnt1 = num, 1\n        elif cnt2 == 0: c2, cnt2 = num, 1\n        else:           cnt1 -= 1; cnt2 -= 1\n    n = len(nums)\n    return [c for c in (c1, c2) if nums.count(c) > n // 3]","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Majority element, find element appearing > n/3 times (two candidates variant)."},{"_id":"69a64ed3ff996647cc9a6c12","name":"Dutch National Flag / 3-Way Partition","category":"Clever Tricks","description":"Dijkstra's 3-way partition: sort an array with 3 distinct values (0/1/2) in-place in one pass using three pointers: low, mid, high.","spaceComplexity":"O(1)","tags":["array","sorting","in-place"],"template":"def sort_colors(nums):\n    \"\"\"Sort array of 0s, 1s, 2s in place.\"\"\"\n    low = mid = 0\n    high = len(nums) - 1\n    while mid <= high:\n        if nums[mid] == 0:\n            nums[low], nums[mid] = nums[mid], nums[low]\n            low += 1; mid += 1\n        elif nums[mid] == 1:\n            mid += 1\n        else:                          # nums[mid] == 2\n            nums[mid], nums[high] = nums[high], nums[mid]\n            high -= 1\n            # Don't advance mid, the swapped element is unknown\n\n# Generic 3-way partition around pivot\ndef three_way_partition(arr, pivot):\n    low = mid = 0\n    high = len(arr) - 1\n    while mid <= high:\n        if arr[mid] < pivot:\n            arr[low], arr[mid] = arr[mid], arr[low]\n            low += 1; mid += 1\n        elif arr[mid] == pivot:\n            mid += 1\n        else:\n            arr[mid], arr[high] = arr[high], arr[mid]\n            high -= 1\n    return low, mid    # [0..low) < pivot, [low..mid) == pivot, [mid..] > pivot","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Sort colors (Leetcode 75), 3-way quicksort pivot partition, partition array by multiple conditions."},{"_id":"69a64ed3ff996647cc9a6c13","name":"QuickSelect","category":"Clever Tricks","description":"Find the k-th smallest element in average O(n) using the partition step from QuickSort.","spaceComplexity":"O(log n) average for recursion","tags":["array","selection","divide-and-conquer"],"template":"import random\n\ndef quick_select(nums, k):\n    \"\"\"Returns the k-th smallest element (0-indexed k).\"\"\"\n    def partition(lo, hi):\n        pivot_idx = random.randint(lo, hi)\n        nums[pivot_idx], nums[hi] = nums[hi], nums[pivot_idx]\n        pivot = nums[hi]\n        i = lo\n        for j in range(lo, hi):\n            if nums[j] <= pivot:\n                nums[i], nums[j] = nums[j], nums[i]\n                i += 1\n        nums[i], nums[hi] = nums[hi], nums[i]\n        return i\n\n    lo, hi = 0, len(nums) - 1\n    while lo < hi:\n        p = partition(lo, hi)\n        if p == k:   return nums[p]\n        elif p < k:  lo = p + 1\n        else:        hi = p - 1\n    return nums[lo]\n\n# K-th largest = (n-k)-th smallest\ndef kth_largest(nums, k):\n    return quick_select(nums, len(nums) - k)","timeComplexity":"O(n) average, O(n²) worst (use random pivot)","updatedAt":{},"whenToUse":"K-th largest/smallest element, median of unsorted array."},{"_id":"69a64ed3ff996647cc9a6c14","name":"Reservoir Sampling","category":"Clever Tricks","description":"Uniformly sample k items from a stream of unknown length without storing the whole stream.","spaceComplexity":"O(k)","tags":["randomization","streaming"],"template":"import random\n\ndef reservoir_sample(stream, k):\n    \"\"\"Pick k items uniformly at random from an iterator.\"\"\"\n    reservoir = []\n    for i, item in enumerate(stream):\n        if i < k:\n            reservoir.append(item)\n        else:\n            j = random.randint(0, i)      # random index in [0..i]\n            if j < k:\n                reservoir[j] = item       # replace with probability k/(i+1)\n    return reservoir\n\n# k=1: pick one element uniformly at random\ndef random_node(head):\n    \"\"\"Pick a random node from a linked list in O(n) time O(1) space.\"\"\"\n    result, i = head, 1\n    node = head.next\n    while node:\n        i += 1\n        if random.randint(1, i) == 1:\n            result = node\n        node = node.next\n    return result","timeComplexity":"O(n)","updatedAt":{},"whenToUse":"Random sample from unknown-size stream, random node in linked list, random index of max element."},{"_id":"69a64ed3ff996647cc9a6c15","name":"Sweep Line","category":"Clever Tricks","description":"Process events sorted by position (left to right). Track active intervals/points using a sorted structure or heap.","spaceComplexity":"O(n)","tags":["geometry","intervals","events"],"template":"# Maximum overlap at any point (e.g. meeting rooms)\ndef max_overlap(intervals):\n    events = []\n    for start, end in intervals:\n        events.append((start, 1))    # +1 at start\n        events.append((end,  -1))   # -1 at end\n    events.sort(key=lambda x: (x[0], x[1]))  # tie-break: end before start\n    active = max_active = 0\n    for _, delta in events:\n        active += delta\n        max_active = max(max_active, active)\n    return max_active\n\n# Skyline problem\nimport heapq\ndef get_skyline(buildings):\n    events = []\n    for l, r, h in buildings:\n        events.append((l, -h, r))   # entering (negative height for max-heap trick)\n        events.append((r,  0, 0))   # leaving\n    events.sort()\n    result = []\n    heap = [(0, float('inf'))]      # (neg_height, right_boundary)\n    for x, neg_h, r in events:\n        if neg_h != 0:\n            heapq.heappush(heap, (neg_h, r))\n        while heap[0][1] <= x:\n            heapq.heappop(heap)\n        max_h = -heap[0][0]\n        if not result or result[-1][1] != max_h:\n            result.append([x, max_h])\n    return result","timeComplexity":"O(n log n)","updatedAt":{},"whenToUse":"Area of rectangles, rectangle overlap, skyline problem, maximum CPU load, minimum platforms."}]